Preciso confirmar se a correção do #349 (entrega de vídeo com HTTP Range), já publicada em produção, está realmente lendo apenas a faixa pedida do Object Storage, e não caindo no caminho antigo. Contexto: a rota de vídeo lia o objeto inteiro num Buffer e fatiava para responder Range; agora usa downloadAsStream(key, {start, end}), aproveitando que a SDK @replit/object-storage@1.0.0 repassa options ao createReadStream do GCS. Esse repasse não é documentado, então preciso vê-lo funcionando em runtime — ler o código não basta. Atenção a uma armadilha: o seek funcionar no player NÃO distingue os casos. Se o objeto não tiver tamanho registrado na tabela object_sizes (migration 080), a rota serve o arquivo inteiro com 200 e o navegador navega localmente; a barra funciona igual. O que distingue é o código HTTP da resposta. Por favor, verifique quatro coisas e me mande os resultados. PRIMEIRO: a migration 080 rodou em produção? Confirme que a tabela object_sizes existe — se as migrations não foram aplicadas no deploy, a rota de vídeo quebra inteira, não só o Range. SEGUNDO: os uploads novos estão registrando o tamanho? Rode SELECT count(*) FROM object_sizes; e depois SELECT object_key, bytes, created_at FROM object_sizes ORDER BY created_at DESC LIMIT 5; Espero ver entradas recentes, incluindo a prova oral feita hoje. Se a tabela estiver vazia, o setObjectSize não está sendo chamado e o resto não importa. TERCEIRO, que é o teste decisivo: numa submissão que tenha vídeo com tamanho registrado, rode curl -s -D - -o /dev/null -H "Range: bytes=1000-1999" "https://super-ta.replit.app/w/WORK_TOKEN/submissions/SUB_TOKEN/proctor-video/0" e me mande os cabeçalhos completos. O esperado é HTTP/1.1 206 Partial Content, com Content-Range: bytes 1000-1999/TOTAL e Content-Length: 1000. Se vier 200, o objeto não tem tamanho registrado e o item 2 explica. Se vier 206 mas o corpo trouxer mais que 1000 bytes, o passthrough {start, end} foi ignorado pela SDK e o storage devolveu o objeto inteiro — esse é o único cenário que exige ação, e é justamente o que eu não consigo testar fora do Replit. QUARTO: repita o mesmo teste para o vídeo consolidado da prova oral, que é o caminho que o player realmente usa e cujo tamanho é gravado em lib/videoConsolidate.js e não no upload: curl -s -D - -o /dev/null -H "Range: bytes=1000-1999" "https://super-ta.replit.app/w/WORK_TOKEN/oral/video/SUB_TOKEN" sem passar o índice, porque é a ausência dele que dispara a consolidação. Se puder, acrescente uma leitura de memória: o RSS do Node em Administrador > Operações antes e depois de reproduzir um vídeo longo. Antes da correção, um vídeo de 40 minutos fazia o processo alocar cerca de 120 MB por requisição; agora deve ficar praticamente estável. Se algum item falhar, me mande os cabeçalhos completos e o trecho de log correspondente.
